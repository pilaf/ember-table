{{#if controller.hasHeader}}
  {{view 'em-table-header-table-container'}}
{{/if}}
{{view 'em-table-body-table-container'}}
{{#if controller.hasFooter}}
  {{view 'em-table-footer-table-container'}}
{{/if}}
{{view 'em-table-scroll-container'}}
{{view 'em-table-column-sortable-indicator'}}
